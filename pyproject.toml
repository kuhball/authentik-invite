[project]
name = "authentik-invite-app"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "authentik-client>=2025.10.2",
    "flask>=3.1.2",
    "flask-oidc>=2.4.0",
    "gunicorn>=23.0.0",
    "pydantic>=2.12.5",
]
