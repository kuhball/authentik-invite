<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>Invite</title>
<style>
body {
font-family: Arial, sans-serif;
display: flex;
flex-direction: column;
align-items: center;
margin: 20px;
max-width: 100%;
box-sizing: border-box;
}

h1 {
font-size: 1.8rem;
margin-bottom: 20px;
}

.link-container {
margin: 20px 0;
display: flex;
flex-direction: column;
align-items: center;
width: 100%;
gap: 10px;
}

#invite-link {
width: 100%;
max-width: 300px;
padding: 12px;
box-sizing: border-box;
font-size: 1rem;
border: 1px solid #ddd;
border-radius: 4px;
}

button {
padding: 12px 16px;
font-size: 1rem;
cursor: pointer;
background-color: #007bff;
color: white;
border: none;
border-radius: 4px;
transition: background-color 0.3s;
width: 100%;
max-width: 300px;
}

.success {
background-color: #28a745;
}

p {
text-align: center;
font-size: 0.9rem;
color: #666;
margin-top: 10px;
}

@media (max-width: 480px) {
h1 {
font-size: 1.5rem;
}

.link-container {
margin: 15px 0;
}

#invite-link, button {
max-width: 100%;
width: 100%;
}

p {
font-size: 0.85rem;
padding: 0 10px;
}
}
</style>
</head>
<body>
<h1>Invite a user to {{ domain }} infrastructure</h1>
<div class="link-container">
<input type="text" id="invite-link" value="https://{{ authentik_url }}/if/flow/default-enrollment-flow/?itoken={{ token }}" readonly>
<button id="copy-button" onclick="copyLink()">Copy Link</button>
</div>
<p>This link is valid until: <span id="expiry-time">{{ valid_until }}</span></p>

<script>
function copyLink() {
const linkInput = document.getElementById('invite-link');
const copyButton = document.getElementById('copy-button');

linkInput.select();
document.execCommand('copy');

// Subtle feedback
copyButton.textContent = 'Copied!';
copyButton.classList.add('success');

setTimeout(() => {
copyButton.textContent = 'Copy Link';
copyButton.classList.remove('success');
}, 2000);
}
</script>
</body>
</html>
